# 目标

修复 `src/a.py` 中 `add` 函数的逻辑错误，使其返回正确的加法结果。

# 当前行为

`add(1, 2)` 返回 `-1`，原因是函数体使用了减法运算符 `-` 而非加法运算符 `+`。

# 复现步骤

1. 在项目根目录执行 `python -c "from src.a import add; print(add(1, 2))"`
2. 观察输出为 `-1`（期望值为 `3`）

# 错误信息

无运行时错误抛出，属于逻辑错误。

# 期望行为

`add(a, b)` 应返回 `a + b`，即两个参数的正确算术和。

# 验收标准

- `add(1, 2) == 3`
- `add(-1, 1) == 0`
- `add(0, 0) == 0`

# 约束

- 不修改函数签名 `def add(a, b)`
- 使用 Python 3.11+ 语法
- 不引入新的第三方依赖
